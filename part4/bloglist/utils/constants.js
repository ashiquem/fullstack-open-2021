const PASS_MIN_LENGTH = 3;
const USERNAME_MIN_LENGTH = 3;

const URLS = {
  blogs: '/api/blogs',
  users: '/api/users',
  login: '/api/login',
};

module.exports = {
  PASS_MIN_LENGTH,
  USERNAME_MIN_LENGTH,
  URLS,
};
